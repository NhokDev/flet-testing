[project]
name = "flet-testing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NhokDev", email = "kleberreyes95@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flet[all]==0.86.5",
]

[dependency-groups]
dev = [
    "flet[test]==0.86.5",
    "pytest==9.1.1",
    "ruff==0.16.3",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]